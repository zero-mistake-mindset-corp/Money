using Money.BL.Models.Transfer;

namespace Money.BL.Interfaces.Transfers;

public interface ITransferService
{
    //Task CreateTransferAsync(CreateTransferModel model, Guid userId);
    //Task<List<TransferModel>> GetAllTransfersAsync (Guid userId, int pageIndex, int pageSize);
    //Task UpdateTransferInfoAsync (UpdateTransferModel model, Guid userId, Guid transferId);
    //Task DeleteTransferAsync (Guid userId, Guid transferId);
}
